// For in loop

const myObj = {
    js: "Javascript",
    cpp: "C plus plus",
    rb: "Ruby",
    py: "Python"
}

for (const key in myObj) {
    // console.log(`${key} has value: ${myObj[key]}`);
}

const arr = ["js", "rb", "html", "py", "css"]
for (const key in arr) {
    // console.log(`${key} has value: ${arr[key]}`);
}

// const map = new Map();
// map.set("IN", "INDIA");
// map.set("USA", "UNITED STATE OF AMERICA");
// map.set("FR", "FRANCE");
// map.set("IN", "INDIA");

// for (const key in map) {
//     console.log(key);
// }


